/**
 * 快速排序
 * @author mulberry
 */

#include <stdio.h>

void quicksort (int arr[]);

/**
 * 快速排序
 * @param {int []} 快速排序数组
 */
void quicksort (int arr[]) {
  int len = sizeof(arr);
  
  for (var i = 0; i < len; i ++) {
    
  }
}
