var http = require('http');
var parser = require('./parser');

var hostname = 'http://www.mulberryx.cn/';
var port = '80';

http.get({
  hostname: hostname,
  port: port,
  path: '/',
  agent: false
}, (res) => {

});

/*
 * 插入排序
 * param {array} 排序对象
 * return {array} 排序结果
 */
module.exports = function () {
	
};